/* 隐藏 canvas 组件，只用于生成海报，不直接显示 */
.poster-canvas-wrap {
  width: 0;
  height: 0;
  overflow: hidden;
  position: fixed;
  left: -9999px;
  top: -9999px;
}

.poster-canvas {
  width: 0;
  height: 0;
  opacity: 0;
}
